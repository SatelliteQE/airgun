Views
=====

Defines view classes for entities (e.g. Hosts → Architectures) and for individual entity (e.g. Hosts → Architectures → x86_64). These classes specifies locators for various widgets (buttons, text input fields...) and also defines helper properties (e.g. is_displayed).

.. contents::
